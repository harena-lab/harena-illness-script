# Documentation

The NER models are divided in two kinds: `Multi Label` and `Multi Class`, where the first , 

## Multi Label

In this model an entity can have multiple labels

## Multi Class

Different from `Multi Label`, in this model the labels are mutually exclusive.
