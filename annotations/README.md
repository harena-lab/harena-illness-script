# Data Sources

* `annotations-medical_specialist-dpoc_groups.csv` - file exported from the DPOC annotations by the medical specialist - selecting two columns: `annotation id` and `self order groups`

  * `categories ordered` - adjacent annotations aggregated by category (originally named as `self order groups`)